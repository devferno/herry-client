import React from 'react'
import {Box} from "@mui/material";

const SearchBar = () => {
  return (
    <Box>
        <Box component="form" sx={{display:"flex",alignItems:"center",padding:3,my:2,justifyContent:"space-between"}}>
            {/* select gender */}
            {/* select category */}]
            {/* search bar  */}
        </Box>
    </Box>

  )
}

export default SearchBar